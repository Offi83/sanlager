/* =========================================================
   Basis
   ========================================================= */

* {
    box-sizing: border-box;
}

html {
    font-family:
        -apple-system,
        BlinkMacSystemFont,
        "Segoe UI",
        sans-serif;
    background: #f4f5f6;
    color: #202124;
}

body {
    margin: 0;
}

a {
    color: inherit;
}

.topbar {
    height: 64px;
    padding: 0 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: #fff;
    border-bottom: 1px solid #ddd;
}

.topbar strong {
    font-size: 18px;
    letter-spacing: .03em;
}

.topbar a {
    color: #333;
    text-decoration: none;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 32px 24px 60px;
}


/* =========================================================
   Seitenkopf
   ========================================================= */

.page-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 24px;
}

.page-header h1 {
    margin: 0 0 6px;
    font-size: 30px;
    line-height: 1.15;
}

.page-header p {
    max-width: 720px;
    margin: 0;
    color: #666;
    line-height: 1.5;
}

.actions {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 10px;
    white-space: nowrap;
}


/* =========================================================
   Karten
   ========================================================= */

.card {
    background: #fff;
    border: 1px solid #ddd;
    border-radius: 12px;
    overflow: hidden;
}

.card + .card {
    margin-top: 20px;
}

.card-header {
    padding: 18px 22px;
    background: #fafafa;
    border-bottom: 1px solid #eee;
}

.card-header h2 {
    margin: 0;
    font-size: 18px;
}


/* =========================================================
   Buttons
   ========================================================= */

.button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 0 18px;
    border: 0;
    border-radius: 8px;
    font: inherit;
    font-weight: 600;
    text-decoration: none;
    cursor: pointer;
}

.button.primary {
    background: #d71920;
    color: #fff;
}

.button.secondary {
    background: #eee;
    color: #222;
}

.button.danger {
    background: #b00020;
    color: #fff;
}

.button.small {
    min-height: 38px;
    padding: 0 14px;
    font-size: 14px;
}


/* =========================================================
   Formulare
   ========================================================= */

input,
select,
textarea {
    width: 100%;
    padding: 13px 14px;
    border: 1px solid #c8c8c8;
    border-radius: 8px;
    background: #fff;
    font: inherit;
    font-size: 16px;
}

input:focus,
select:focus,
textarea:focus {
    outline: 2px solid #d71920;
    outline-offset: 1px;
}

label {
    display: flex;
    flex-direction: column;
    gap: 7px;
}

label > span,
.form-group label {
    font-weight: 600;
}

.form-group {
    display: flex;
    flex-direction: column;
    gap: 7px;
}

.form-group label {
    font-weight: 600;
}

.form-group small {
    color: #777;
}

.form-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 18px;
}

.form-group.wide {
    grid-column: 1 / -1;
}

.form {
    padding: 24px;
}

.form > label {
    margin-top: 20px;
}

.form textarea,
.stock-form textarea {
    resize: vertical;
    min-height: 90px;
}

.form-actions {
    display: flex;
    justify-content: flex-start;
    gap: 10px;
    margin-top: 22px;
    padding-top: 20px;
    border-top: 1px solid #eee;
}

.form-actions .button {
    min-width: 150px;
}

.form-help {
    margin: -10px 0 22px;
    color: #777;
}

.description {
    margin-top: 4px;
    color: #777;
    font-size: 14px;
}


/* =========================================================
   Suche
   ========================================================= */

.search {
    display: flex;
    gap: 10px;
    margin-bottom: 20px;
}

.search input {
    flex: 1;
}


/* =========================================================
   Tabellen
   ========================================================= */

.table-wrapper {
    overflow-x: auto;
}

table {
    width: 100%;
    border-collapse: collapse;
}

th,
td {
    padding: 16px;
    text-align: left;
    border-bottom: 1px solid #eee;
}

th {
    background: #fafafa;
    color: #555;
    font-size: 14px;
}

tbody tr:last-child td {
    border-bottom: 0;
}

.category-row th {
    padding-top: 18px;
    padding-bottom: 8px;
    background: #f7f7f7;
    border-bottom: 2px solid #ddd;
    font-weight: 700;
}

.category-row:first-child th {
    padding-top: 8px;
}

.category-filter {
    min-width: 190px;
}


/* =========================================================
   Meldungen / Zustände
   ========================================================= */

.alert {
    margin-bottom: 20px;
    padding: 14px 18px;
    border-radius: 8px;
}

.alert.success {
    background: #e7f6ea;
    border: 1px solid #a8d5ae;
}

.alert.error {
    background: #fdeaea;
    border: 1px solid #e0a0a0;
}

.warning {
    display: block;
    margin-top: 4px;
    color: #b00020;
    font-size: 13px;
    font-weight: 600;
}

.warning-alert {
    border-left: 5px solid #b00020;
}

.expired-stock-warning {
    display: block;
    margin-top: 5px;
    color: #b00020;
    font-size: 13px;
    font-weight: 700;
}

.empty {
    padding: 70px 30px;
    text-align: center;
}

.empty-icon {
    margin-bottom: 15px;
    font-size: 48px;
}

.empty h2 {
    margin: 0 0 8px;
}

.empty p {
    margin: 0 0 24px;
    color: #777;
}

.small-empty {
    padding: 24px;
}

.empty-state.compact {
    padding: 28px 22px;
    color: #777;
    text-align: center;
}


/* =========================================================
   Bestand
   ========================================================= */

.stock-summary {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin-bottom: 20px;
}

.stock-total,
.stock-minimum {
    padding: 22px;
    background: #fff;
    border: 1px solid #ddd;
    border-radius: 12px;
}

.stock-total span,
.stock-minimum span {
    display: block;
    margin-bottom: 8px;
    color: #666;
}

.stock-total strong,
.stock-minimum strong {
    font-size: 28px;
}

.stock-low {
    color: #b00020;
}

.stock-value {
    font-weight: 700;
}

.location-list {
    padding: 0 22px;
}

.location-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-height: 64px;
    padding: 16px 0;
    border-bottom: 1px solid #eee;
}

.location-row:last-child {
    border-bottom: 0;
}

.location-row strong {
    font-size: 18px;
}


/* =========================================================
   MHD / Chargen
   ========================================================= */

.expiry-ok {
    color: #287a3d;
    font-weight: 600;
}

.expiry-warning {
    color: #9a6700;
    font-weight: 700;
}

.expiry-expired {
    color: #b00020;
    font-weight: 700;
}


/* =========================================================
   Artikeldetail
   ========================================================= */

.article-detail-page .back-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 8px;
    color: #666;
    font-size: 14px;
    font-weight: 600;
    text-decoration: none;
}

.article-detail-page .back-link:hover {
    color: #d71920;
}

.article-detail-page .stock-summary {
    grid-template-columns: minmax(0, 1.5fr) minmax(220px, 1fr);
    gap: 18px;
    margin-bottom: 22px;
}

.article-detail-page .stock-total,
.article-detail-page .stock-minimum {
    position: relative;
    padding: 24px 26px;
}

.article-detail-page .stock-total {
    border-left: 4px solid #d71920;
}

.article-detail-page .stock-total span,
.article-detail-page .stock-minimum span {
    margin-bottom: 9px;
    font-size: 13px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: .04em;
}

.article-detail-page .stock-total strong,
.article-detail-page .stock-minimum strong {
    display: block;
    font-size: 32px;
    line-height: 1.1;
}

.article-detail-page .stock-total .warning {
    margin-top: 10px;
}

.article-detail-page .location-row .stock-value {
    font-size: 17px;
}

.article-detail-page .stock-form {
    padding: 22px;
}

.article-detail-page .stock-form > .form-grid {
    grid-template-columns:
        minmax(150px, 1fr)
        minmax(180px, 1.2fr)
        minmax(120px, .7fr)
        minmax(220px, 1.5fr);
    gap: 16px;
}

.article-detail-page .stock-form label > span {
    color: #444;
    font-size: 14px;
    font-weight: 700;
}

.article-detail-page .stock-form > label {
    margin-top: 18px;
}

.article-detail-page .conditional-field {
    margin-top: 18px;
    padding: 16px 18px;
    border: 1px solid #ddd;
    border-radius: 10px;
    background: #fafafa;
}

.article-detail-page .conditional-field .form-help {
    margin: 8px 0 0;
}

.article-detail-page .stock-form .form-actions {
    margin-top: 20px;
    padding-top: 20px;
}

.article-detail-page .stock-form .form-actions .button {
    min-width: 150px;
}


/* =========================================================
   QR-Code
   ========================================================= */

.article-qr-card {
    margin-bottom: 20px;
}

.article-qr-content {
    display: flex;
    align-items: center;
    gap: 24px;
    padding: 22px;
}

.article-qr-code {
    flex: 0 0 180px;
    width: 180px;
    height: 180px;
}

.article-qr-code svg,
.label-qr svg {
    display: block;
    width: 100%;
    height: 100%;
}

.article-qr-info {
    display: flex;
    flex-direction: column;
    gap: 6px;
}

.article-qr-info strong {
    font-size: 18px;
}

.article-qr-info span {
    color: #555;
}

.article-qr-info small {
    margin-top: 8px;
    color: #777;
}


/* =========================================================
   Ausbuchen / Scanner
   ========================================================= */

.issue-card {
    padding: 24px;
}

.issue-form {
    display: flex;
    align-items: flex-end;
    gap: 16px;
}

.issue-form label {
    flex: 1;
}

.issue-form label > span {
    color: #444;
    font-size: 14px;
    font-weight: 700;
}

.issue-form input {
    min-height: 52px;
    font-size: 20px;
}

.issue-form .button {
    min-width: 160px;
    min-height: 52px;
}

.issue-scan-button {
    margin-bottom: 16px;
}

.issue-scanner {
    width: 100%;
    max-width: 420px;
    margin-bottom: 16px;
    overflow: hidden;
    border-radius: 12px;
}

.issue-scanner video {
    display: block;
    width: 100%;
}

.issue-scan-status {
    margin-bottom: 16px;
    padding: 10px 12px;
    border-radius: 8px;
    background: #f1f3f4;
    color: #444;
}

.scan-result {
    margin-top: 16px;
    padding: 14px 16px;
    border-radius: 8px;
    background: #eaf6ea;
    font-weight: 600;
}

.scan-result.error {
    background: #fbeaea;
}

.today-summary {
    display: flex;
    align-items: baseline;
    gap: 12px;
    padding: 22px;
}

.today-summary strong {
    font-size: 32px;
}

.today-summary span {
    color: #666;
}


/* =========================================================
   Etikettendruck
   ========================================================= */

.label-print-page {
    width: 210mm;
    height: 296mm;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(2, 105mm);
    grid-template-rows: repeat(4, 74mm);
}

.label {
    width: 105mm;
    height: 74mm;
    padding: 7mm;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    border: 0.3mm solid #888;
    background: #fff;
}

.label-category {
    margin-bottom: 4mm;
    color: #555;
    font-size: 11pt;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: .04em;
}

.label-content {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 6mm;
}

.label-text {
    min-width: 0;
    flex: 1;
}

.label-name {
    font-size: 18pt;
    font-weight: 700;
    line-height: 1.15;
    overflow-wrap: anywhere;
}

.label-number {
    margin-top: 5mm;
    color: #555;
    font-size: 11pt;
    font-family: monospace;
}

.label-qr {
    flex: 0 0 35mm;
    width: 35mm;
    height: 35mm;
}

@media screen {
    .label-print-page {
        margin-top: 20px;
        box-shadow: 0 0 0 1px #ddd;
    }
}

@media print {
    @page {
        size: A4 portrait;
        margin: 0;
    }

    html,
    body {
        width: 210mm;
        margin: 0;
        padding: 0;
        background: #fff;
    }

    .topbar,
    .container > *:not(.label-print-page) {
        display: none !important;
    }

    .container {
        width: 210mm;
        max-width: none;
        margin: 0;
        padding: 0;
    }

    .label-print-page {
        width: 210mm;
        height: 296mm;
        margin: 0;
    }

    .label {
        break-inside: avoid;
        page-break-inside: avoid;
    }
}


/* =========================================================
   Responsive
   ========================================================= */

@media (min-width: 701px) {
    .article-detail-page .stock-form input,
    .article-detail-page .stock-form select,
    .form input,
    .form select {
        min-height: 48px;
    }
}

@media (max-width: 900px) {
    .article-detail-page .stock-summary {
        grid-template-columns: 1fr;
    }

    .article-detail-page .stock-form > .form-grid {
        grid-template-columns: 1fr 1fr;
    }

    .form-grid {
        grid-template-columns: 1fr 1fr;
    }
}

@media (max-width: 700px) {
    .container {
        padding: 20px 14px 40px;
    }

    .page-header {
        align-items: stretch;
        flex-direction: column;
    }

    .page-header .button,
    .actions .button {
        width: 100%;
    }

    .actions {
        flex-direction: column;
        align-items: stretch;
        gap: 6px;
    }

    .search {
        flex-wrap: wrap;
    }

    .search input {
        flex-basis: 100%;
    }

    .form {
        padding: 18px;
    }

    .form-grid {
        grid-template-columns: 1fr;
    }

    .form-group.wide {
        grid-column: auto;
    }

    .form-actions {
        flex-direction: column;
    }

    .form-actions .button {
        width: 100%;
    }

    .table-wrapper {
        overflow-x: auto;
    }

    th,
    td {
        padding: 13px 12px;
    }

    .stock-summary {
        grid-template-columns: 1fr;
    }

    .article-detail-page .stock-total,
    .article-detail-page .stock-minimum {
        padding: 20px;
    }

    .article-detail-page .stock-total strong,
    .article-detail-page .stock-minimum strong {
        font-size: 28px;
    }

    .article-detail-page .stock-form {
        padding: 18px;
    }

    .article-detail-page .stock-form > .form-grid {
        grid-template-columns: 1fr;
    }

    .issue-card {
        padding: 18px;
    }

    .issue-form {
        align-items: stretch;
        flex-direction: column;
    }

    .issue-form .button {
        width: 100%;
    }

    .article-qr-content {
        flex-direction: column;
        align-items: flex-start;
    }

    .article-qr-code {
        width: 160px;
        height: 160px;
    }

    .today-summary {
        padding: 18px;
    }
}
